version: '3'
services:
  ktor-users:
    image: jpgcz/ktor-users:${APP_VERSION:-dev}
    ports:
      - "8081:8080"
    environment:
      - ENVIRONMENT=development
      - APP_VERSION=${APP_VERSION:-0.1.0}
